<mvc:View
    controllerName="starwarsfront.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
>
    <Page
        id="page"
        title="{i18n>title}"
    >
        <content>
            <Text text="ui5 dentro de proyecto cap"  />
            <Button text="go to cataglof" press=".onpress"></Button>
            <List items="{/Products}">
                <StandardListItem
                    title="{id}"
                    description="{description}"
                />
            </List>
        </content>

    </Page>
</mvc:View>
